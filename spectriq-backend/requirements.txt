# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Config ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0


# --- Transcription + summarization ---
groq==0.15.0
requests==2.32.3

# --- Chat with meeting (RAG) ---
pgvector==0.3.6       # SQLAlchemy Vector type + pgvector comparators
google-genai==1.3.0    # Gemini API - embeddings + chat, both free-tier friendly

# --- Auth (Clerk session token verification) ---
pyjwt[crypto]==2.10.1

# --- Misc ---
python-dotenv==1.0.1

# --- Object storage (only used when STORAGE_BACKEND=s3) ---
boto3==1.35.90
